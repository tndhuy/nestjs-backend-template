---
phase: 01-template-scaffold
plan: 02
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - nestjs-backend-template/src/example/domain/item.entity.ts
  - nestjs-backend-template/src/example/domain/item-name.value-object.ts
  - nestjs-backend-template/src/example/domain/item.repository.interface.ts
  - nestjs-backend-template/src/example/application/commands/create-item.command.ts
  - nestjs-backend-template/src/example/application/commands/create-item.handler.ts
  - nestjs-backend-template/src/example/application/commands/delete-item.command.ts
  - nestjs-backend-template/src/example/application/commands/delete-item.handler.ts
  - nestjs-backend-template/src/example/application/queries/get-item.query.ts
  - nestjs-backend-template/src/example/application/queries/get-item.handler.ts
  - nestjs-backend-template/src/example/application/queries/list-items.query.ts
  - nestjs-backend-template/src/example/application/queries/list-items.handler.ts
  - nestjs-backend-template/src/example/application/dtos/create-item.dto.ts
  - nestjs-backend-template/src/example/application/dtos/item.response.dto.ts
  - nestjs-backend-template/src/example/infrastructure/.gitkeep
autonomous: true
requirements:
  - REQ-001
must_haves:
  truths:
    - "Item entity extends custom AggregateRoot (not @nestjs/cqrs AggregateRoot)"
    - "ItemName value object validates non-empty strings"
    - "IItemRepository interface defines findById, findAll, save, delete"
    - "All 4 command/query handlers exist and throw NotImplementedException"
    - "Commands and queries are plain classes with readonly properties"
  artifacts:
    - path: "nestjs-backend-template/src/example/domain/item.entity.ts"
      provides: "Item aggregate root entity"
      contains: "extends AggregateRoot<string>"
    - path: "nestjs-backend-template/src/example/domain/item-name.value-object.ts"
      provides: "ItemName value object"
      contains: "extends ValueObject"
    - path: "nestjs-backend-template/src/example/domain/item.repository.interface.ts"
      provides: "IItemRepository port interface"
      contains: "export interface IItemRepository"
    - path: "nestjs-backend-template/src/example/application/commands/create-item.handler.ts"
      provides: "CreateItem command handler"
      contains: "@CommandHandler(CreateItemCommand)"
    - path: "nestjs-backend-template/src/example/application/queries/get-item.handler.ts"
      provides: "GetItem query handler"
      contains: "@QueryHandler(GetItemQuery)"
  key_links:
    - from: "nestjs-backend-template/src/example/domain/item.entity.ts"
      to: "nestjs-backend-template/src/shared/base/aggregate-root.ts"
      via: "extends AggregateRoot"
      pattern: "import.*AggregateRoot.*from.*shared/base"
    - from: "nestjs-backend-template/src/example/domain/item-name.value-object.ts"
      to: "nestjs-backend-template/src/shared/base/value-object.ts"
      via: "extends ValueObject"
      pattern: "import.*ValueObject.*from.*shared/base"
---

<objective>
Create the ExampleModule domain and application layers with full CQRS structure.

Purpose: Demonstrate the DDD + CQRS pattern that teams will replicate when creating new domain modules. This is the core educational artifact of the template — Item domain (entity + value object + repository interface) plus all command/query handlers.

Output: Complete domain/ and application/ layers for ExampleModule, all compiling cleanly.
</objective>

<execution_context>
@.planning/phases/01-template-scaffold/01-CONTEXT.md
@.planning/phases/01-template-scaffold/01-RESEARCH.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md

<interfaces>
<!-- From Plan 01 — shared base classes the executor MUST use -->

From nestjs-backend-template/src/shared/base/aggregate-root.ts:
```typescript
export abstract class AggregateRoot<TId> {
  readonly id: TId;
  protected constructor(id: TId);
  addDomainEvent(event: DomainEvent): void;
  pullDomainEvents(): DomainEvent[];
}
```

From nestjs-backend-template/src/shared/base/value-object.ts:
```typescript
export abstract class ValueObject<TProps> {
  protected readonly props: TProps;
  protected constructor(props: TProps);
  equals(other?: ValueObject<TProps>): boolean;
}
```

From nestjs-backend-template/src/shared/base/domain-event.ts:
```typescript
export abstract class DomainEvent {
  readonly occurredAt: Date;
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Item domain layer (entity, value object, repository interface)</name>
  <files>
    nestjs-backend-template/src/example/domain/item.entity.ts
    nestjs-backend-template/src/example/domain/item-name.value-object.ts
    nestjs-backend-template/src/example/domain/item.repository.interface.ts
    nestjs-backend-template/src/example/infrastructure/.gitkeep
  </files>
  <read_first>
    nestjs-backend-template/src/shared/base/aggregate-root.ts (base class Item extends)
    nestjs-backend-template/src/shared/base/value-object.ts (base class ItemName extends)
    .planning/phases/01-template-scaffold/01-RESEARCH.md (Pattern 6: Item domain model — exact code)
    .planning/phases/01-template-scaffold/01-CONTEXT.md (decision B.2: Item domain, anti-pattern: never import AggregateRoot from @nestjs/cqrs)
  </read_first>
  <action>
    Per decision B.2 (Item domain with DDD) and Research Pattern 6:

    1. Create directory structure:
       ```
       nestjs-backend-template/src/example/domain/
       nestjs-backend-template/src/example/infrastructure/
       ```

    2. Create `src/example/domain/item-name.value-object.ts`:
       ```typescript
       import { ValueObject } from '../../shared/base/value-object';

       interface ItemNameProps {
         value: string;
       }

       export class ItemName extends ValueObject<ItemNameProps> {
         static create(value: string): ItemName {
           if (!value || value.trim().length === 0) {
             throw new Error('ItemName cannot be empty');
           }
           return new ItemName({ value: value.trim() });
         }

         get value(): string {
           return this.props.value;
         }
       }
       ```

    3. Create `src/example/domain/item.entity.ts`:
       ```typescript
       import { AggregateRoot } from '../../shared/base/aggregate-root';
       import { ItemName } from './item-name.value-object';

       export class Item extends AggregateRoot<string> {
         private _name: ItemName;

         private constructor(id: string, name: ItemName) {
           super(id);
           this._name = name;
         }

         static create(id: string, name: ItemName): Item {
           return new Item(id, name);
         }

         get name(): ItemName {
           return this._name;
         }
       }
       ```

    4. Create `src/example/domain/item.repository.interface.ts`:
       ```typescript
       import { Item } from './item.entity';

       export const ITEM_REPOSITORY = Symbol('IItemRepository');

       export interface IItemRepository {
         findById(id: string): Promise<Item | null>;
         findAll(): Promise<Item[]>;
         save(item: Item): Promise<void>;
         delete(id: string): Promise<void>;
       }
       ```

    5. Create `src/example/infrastructure/.gitkeep` (empty file — infrastructure adapter added in Phase 2).

    CRITICAL: Import AggregateRoot from `../../shared/base/aggregate-root`, NOT from `@nestjs/cqrs`.
  </action>
  <verify>
    <automated>cd /Users/trannguyendanghuy/Workspace/happyland-of-draken/nestjs-backend-template && pnpm build</automated>
  </verify>
  <acceptance_criteria>
    - nestjs-backend-template/src/example/domain/item.entity.ts contains `import { AggregateRoot } from '../../shared/base/aggregate-root'`
    - nestjs-backend-template/src/example/domain/item.entity.ts contains `export class Item extends AggregateRoot<string>`
    - nestjs-backend-template/src/example/domain/item.entity.ts contains `static create(id: string, name: ItemName): Item`
    - nestjs-backend-template/src/example/domain/item.entity.ts does NOT contain `from '@nestjs/cqrs'`
    - nestjs-backend-template/src/example/domain/item-name.value-object.ts contains `export class ItemName extends ValueObject<ItemNameProps>`
    - nestjs-backend-template/src/example/domain/item-name.value-object.ts contains `throw new Error('ItemName cannot be empty')`
    - nestjs-backend-template/src/example/domain/item.repository.interface.ts contains `export const ITEM_REPOSITORY = Symbol('IItemRepository')`
    - nestjs-backend-template/src/example/domain/item.repository.interface.ts contains `findById(id: string): Promise<Item | null>`
    - nestjs-backend-template/src/example/domain/item.repository.interface.ts contains `save(item: Item): Promise<void>`
    - nestjs-backend-template/src/example/infrastructure/.gitkeep exists
    - `pnpm build` exits with code 0
  </acceptance_criteria>
  <done>Item entity extends custom AggregateRoot, ItemName value object validates non-empty strings, IItemRepository interface defines CRUD port. infrastructure/ has .gitkeep only. All compiles.</done>
</task>

<task type="auto">
  <name>Task 2: Create CQRS application layer (commands, queries, DTOs)</name>
  <files>
    nestjs-backend-template/src/example/application/commands/create-item.command.ts
    nestjs-backend-template/src/example/application/commands/create-item.handler.ts
    nestjs-backend-template/src/example/application/commands/delete-item.command.ts
    nestjs-backend-template/src/example/application/commands/delete-item.handler.ts
    nestjs-backend-template/src/example/application/queries/get-item.query.ts
    nestjs-backend-template/src/example/application/queries/get-item.handler.ts
    nestjs-backend-template/src/example/application/queries/list-items.query.ts
    nestjs-backend-template/src/example/application/queries/list-items.handler.ts
    nestjs-backend-template/src/example/application/dtos/create-item.dto.ts
    nestjs-backend-template/src/example/application/dtos/item.response.dto.ts
  </files>
  <read_first>
    .planning/phases/01-template-scaffold/01-RESEARCH.md (Pattern 2: Command Handler with NotImplementedException, Pattern 3: Query Handler)
    .planning/phases/01-template-scaffold/01-CONTEXT.md (decision B.1: no stub repos, decision B.2: CQRS commands/queries list, decision C: handlers throw NotImplementedException)
  </read_first>
  <action>
    Per decisions B.1, B.2, and C — all handlers throw NotImplementedException until Phase 2 wires real repo.

    1. Create directory structure:
       ```
       nestjs-backend-template/src/example/application/commands/
       nestjs-backend-template/src/example/application/queries/
       nestjs-backend-template/src/example/application/dtos/
       ```

    2. Create `src/example/application/commands/create-item.command.ts`:
       ```typescript
       export class CreateItemCommand {
         constructor(public readonly name: string) {}
       }
       ```

    3. Create `src/example/application/commands/create-item.handler.ts`:
       ```typescript
       import { CommandHandler, ICommandHandler } from '@nestjs/cqrs';
       import { NotImplementedException } from '@nestjs/common';
       import { CreateItemCommand } from './create-item.command';

       @CommandHandler(CreateItemCommand)
       export class CreateItemHandler implements ICommandHandler<CreateItemCommand> {
         async execute(_command: CreateItemCommand): Promise<void> {
           throw new NotImplementedException('CreateItemHandler: wired in Phase 2');
         }
       }
       ```

    4. Create `src/example/application/commands/delete-item.command.ts`:
       ```typescript
       export class DeleteItemCommand {
         constructor(public readonly id: string) {}
       }
       ```

    5. Create `src/example/application/commands/delete-item.handler.ts`:
       ```typescript
       import { CommandHandler, ICommandHandler } from '@nestjs/cqrs';
       import { NotImplementedException } from '@nestjs/common';
       import { DeleteItemCommand } from './delete-item.command';

       @CommandHandler(DeleteItemCommand)
       export class DeleteItemHandler implements ICommandHandler<DeleteItemCommand> {
         async execute(_command: DeleteItemCommand): Promise<void> {
           throw new NotImplementedException('DeleteItemHandler: wired in Phase 2');
         }
       }
       ```

    6. Create `src/example/application/queries/get-item.query.ts`:
       ```typescript
       export class GetItemQuery {
         constructor(public readonly id: string) {}
       }
       ```

    7. Create `src/example/application/queries/get-item.handler.ts`:
       ```typescript
       import { IQueryHandler, QueryHandler } from '@nestjs/cqrs';
       import { NotImplementedException } from '@nestjs/common';
       import { GetItemQuery } from './get-item.query';

       @QueryHandler(GetItemQuery)
       export class GetItemHandler implements IQueryHandler<GetItemQuery> {
         async execute(_query: GetItemQuery): Promise<unknown> {
           throw new NotImplementedException('GetItemHandler: wired in Phase 2');
         }
       }
       ```

    8. Create `src/example/application/queries/list-items.query.ts`:
       ```typescript
       export class ListItemsQuery {
         constructor() {}
       }
       ```

    9. Create `src/example/application/queries/list-items.handler.ts`:
       ```typescript
       import { IQueryHandler, QueryHandler } from '@nestjs/cqrs';
       import { NotImplementedException } from '@nestjs/common';
       import { ListItemsQuery } from './list-items.query';

       @QueryHandler(ListItemsQuery)
       export class ListItemsHandler implements IQueryHandler<ListItemsQuery> {
         async execute(_query: ListItemsQuery): Promise<unknown> {
           throw new NotImplementedException('ListItemsHandler: wired in Phase 2');
         }
       }
       ```

    10. Create `src/example/application/dtos/create-item.dto.ts`:
        ```typescript
        export class CreateItemDto {
          name: string;
        }
        ```

    11. Create `src/example/application/dtos/item.response.dto.ts`:
        ```typescript
        export class ItemResponseDto {
          id: string;
          name: string;
        }
        ```
  </action>
  <verify>
    <automated>cd /Users/trannguyendanghuy/Workspace/happyland-of-draken/nestjs-backend-template && pnpm build</automated>
  </verify>
  <acceptance_criteria>
    - nestjs-backend-template/src/example/application/commands/create-item.command.ts contains `export class CreateItemCommand`
    - nestjs-backend-template/src/example/application/commands/create-item.command.ts contains `public readonly name: string`
    - nestjs-backend-template/src/example/application/commands/create-item.handler.ts contains `@CommandHandler(CreateItemCommand)`
    - nestjs-backend-template/src/example/application/commands/create-item.handler.ts contains `throw new NotImplementedException`
    - nestjs-backend-template/src/example/application/commands/delete-item.command.ts contains `export class DeleteItemCommand`
    - nestjs-backend-template/src/example/application/commands/delete-item.handler.ts contains `@CommandHandler(DeleteItemCommand)`
    - nestjs-backend-template/src/example/application/queries/get-item.query.ts contains `export class GetItemQuery`
    - nestjs-backend-template/src/example/application/queries/get-item.handler.ts contains `@QueryHandler(GetItemQuery)`
    - nestjs-backend-template/src/example/application/queries/list-items.query.ts contains `export class ListItemsQuery`
    - nestjs-backend-template/src/example/application/queries/list-items.handler.ts contains `@QueryHandler(ListItemsQuery)`
    - nestjs-backend-template/src/example/application/dtos/create-item.dto.ts contains `export class CreateItemDto`
    - nestjs-backend-template/src/example/application/dtos/item.response.dto.ts contains `export class ItemResponseDto`
    - All 4 handler files contain `NotImplementedException`
    - `pnpm build` exits with code 0
  </acceptance_criteria>
  <done>All 4 CQRS handlers (CreateItem, DeleteItem, GetItem, ListItems) exist with @CommandHandler/@QueryHandler decorators and throw NotImplementedException. DTOs for create request and item response exist. All compiles.</done>
</task>

</tasks>

<verification>
- `pnpm build` passes
- `ls src/example/application/commands/` shows 4 files (2 commands + 2 handlers)
- `ls src/example/application/queries/` shows 4 files (2 queries + 2 handlers)
- `ls src/example/application/dtos/` shows 2 files
- `ls src/example/domain/` shows 3 files
- `grep -r "NotImplementedException" src/example/application/` returns 4 matches (one per handler)
- `grep -r "from '@nestjs/cqrs'" src/example/domain/` returns 0 matches (domain layer must not import framework)
</verification>

<success_criteria>
- Item entity extends custom AggregateRoot (not @nestjs/cqrs), ItemName validates non-empty
- IItemRepository interface defines findById, findAll, save, delete
- 2 command handlers + 2 query handlers all decorated and throwing NotImplementedException
- DTOs exist for create input and item response
- infrastructure/ contains only .gitkeep
- pnpm build passes
</success_criteria>

<output>
After completion, create `.planning/phases/01-template-scaffold/01-02-SUMMARY.md`
</output>
