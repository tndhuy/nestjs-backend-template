---
phase: 04-agent-configs-documentation
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - .agent/context/ARCHITECTURE.md
  - .agent/context/STACK.md
  - .agent/context/CONVENTIONS.md
  - .agent/skills/gsd/SKILL.md
  - .agent/mcp/mcp.json
  - .agent/README.md
autonomous: true
requirements: [REQ-008]

must_haves:
  truths:
    - ".agent/ directory has context/, skills/, mcp/ subdirectories with populated files"
    - "Context files mirror .planning/codebase/ content for architecture, stack, and conventions"
    - "MCP config mirrors project .mcp.json servers (context7, prisma, playwright)"
    - "README explains how to configure Antigravity for this project"
  artifacts:
    - path: ".agent/context/ARCHITECTURE.md"
      provides: "DDD architecture for Antigravity"
      min_lines: 10
    - path: ".agent/context/STACK.md"
      provides: "Tech stack for Antigravity"
      min_lines: 10
    - path: ".agent/context/CONVENTIONS.md"
      provides: "Coding conventions for Antigravity"
      min_lines: 10
    - path: ".agent/skills/gsd/SKILL.md"
      provides: "GSD skill for Antigravity"
      contains: "name: gsd"
    - path: ".agent/mcp/mcp.json"
      provides: "MCP server config for Antigravity"
      contains: "context7"
    - path: ".agent/README.md"
      provides: "Antigravity setup instructions"
      contains: "Antigravity"
  key_links:
    - from: ".agent/context/ARCHITECTURE.md"
      to: ".planning/codebase/ARCHITECTURE.md"
      via: "content mirror"
      pattern: "DDD"
    - from: ".agent/mcp/mcp.json"
      to: ".mcp.json"
      via: "same server config"
      pattern: "context7"
---

<objective>
Scaffold the `.agent/` directory for Antigravity agent: context files (mirroring .planning/codebase/), GSD skill stub, MCP config, and README.

Purpose: Antigravity agent gets the same project context as Claude Code — DDD architecture, conventions, MCP servers — without manual configuration.

Output: .agent/context/ (3 files), .agent/skills/gsd/SKILL.md, .agent/mcp/mcp.json, .agent/README.md
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-agent-configs-documentation/04-CONTEXT.md
@.planning/phases/04-agent-configs-documentation/04-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create .agent/ directory structure with context, skills, MCP, and README</name>
  <files>.agent/context/ARCHITECTURE.md, .agent/context/STACK.md, .agent/context/CONVENTIONS.md, .agent/skills/gsd/SKILL.md, .agent/mcp/mcp.json, .agent/README.md</files>
  <read_first>
    - .planning/codebase/ARCHITECTURE.md (source content to copy into .agent/context/ARCHITECTURE.md)
    - .planning/codebase/STACK.md (source content to copy into .agent/context/STACK.md)
    - .planning/codebase/CONVENTIONS.md (source content to copy into .agent/context/CONVENTIONS.md)
    - .planning/phases/04-agent-configs-documentation/04-RESEARCH.md (GSD SKILL.md format, .agent/ directory convention, mcp.json structure)
  </read_first>
  <action>
    **Create directory structure:**
    ```
    .agent/
    ├── context/
    │   ├── ARCHITECTURE.md
    │   ├── STACK.md
    │   └── CONVENTIONS.md
    ├── skills/
    │   └── gsd/
    │       └── SKILL.md
    ├── mcp/
    │   └── mcp.json
    └── README.md
    ```

    **`.agent/context/ARCHITECTURE.md`** — Copy the full content of `.planning/codebase/ARCHITECTURE.md` verbatim. This is the DDD layer architecture reference. Do NOT modify or summarize.

    **`.agent/context/STACK.md`** — Copy the full content of `.planning/codebase/STACK.md` verbatim. This is the tech stack reference. Do NOT modify or summarize.

    **`.agent/context/CONVENTIONS.md`** — Copy the full content of `.planning/codebase/CONVENTIONS.md` verbatim. This is the naming and coding conventions reference. Do NOT modify or summarize.

    **`.agent/skills/gsd/SKILL.md`** — Same content as `.claude/skills/gsd/SKILL.md`:
    ```markdown
    ---
    name: gsd
    description: Use for milestone-based development workflows. Invoke /gsd:<command> for planning, research, implementation, and verification of project phases.
    ---

    # GSD Skill

    GSD (Get Shit Done) is a milestone-driven development framework installed globally.

    ## Commands
    - `/gsd:map-codebase` -- map project structure into `.planning/codebase/`
    - `/gsd:discuss-phase` -- gather requirements + decisions for a phase
    - `/gsd:research-phase` -- research implementation approach
    - `/gsd:plan-phase` -- generate task plans
    - `/gsd:do-work` -- execute plans
    - `/gsd:verify-work` -- verify completion

    State lives in `.planning/`. Never delete `.planning/` files.
    ```

    **`.agent/mcp/mcp.json`** — Same 3 servers as project root `.mcp.json`:
    ```json
    {
      "mcpServers": {
        "context7": {
          "type": "stdio",
          "command": "npx",
          "args": ["-y", "@upstash/context7-mcp"]
        },
        "prisma": {
          "type": "stdio",
          "command": "npx",
          "args": ["-y", "@prisma/mcp-server-prisma"],
          "env": {
            "DATABASE_URL": "${DATABASE_URL}"
          }
        },
        "playwright": {
          "type": "stdio",
          "command": "npx",
          "args": ["-y", "@playwright/mcp"]
        }
      }
    }
    ```

    **`.agent/README.md`** — Write a setup guide with these sections:
    1. `# Antigravity Agent Configuration` — What this directory provides.
    2. `## Directory Structure` — Explain context/, skills/, mcp/ purpose.
    3. `## Context Files` — Explain that ARCHITECTURE.md, STACK.md, CONVENTIONS.md are mirrors of `.planning/codebase/` and should be updated when codebase maps change.
    4. `## MCP Servers` — List the 3 project-level servers (context7, prisma, playwright) and explain they match `.mcp.json` at project root.
    5. `## Setup` — Step-by-step: (1) Ensure `npx` available, (2) Set DATABASE_URL in environment, (3) Antigravity auto-loads from `.agent/` on project open.
    6. `## Keeping Context Fresh` — Run `/gsd:map-codebase` to regenerate `.planning/codebase/` maps, then copy updated files into `.agent/context/`.
  </action>
  <verify>
    <automated>cd /Users/trannguyendanghuy/Workspace/happyland-of-draken/nestjs-backend-template && ls .agent/context/ARCHITECTURE.md .agent/context/STACK.md .agent/context/CONVENTIONS.md .agent/skills/gsd/SKILL.md .agent/mcp/mcp.json .agent/README.md && grep "context7" .agent/mcp/mcp.json && grep "Antigravity" .agent/README.md && grep "name: gsd" .agent/skills/gsd/SKILL.md && wc -l .agent/context/ARCHITECTURE.md</automated>
  </verify>
  <acceptance_criteria>
    - .agent/context/ARCHITECTURE.md exists and has 10+ lines (copied from .planning/codebase/)
    - .agent/context/STACK.md exists and has 10+ lines
    - .agent/context/CONVENTIONS.md exists and has 10+ lines
    - .agent/skills/gsd/SKILL.md exists and contains `name: gsd`
    - .agent/mcp/mcp.json exists, is valid JSON, and contains `context7`, `prisma`, `playwright`
    - .agent/mcp/mcp.json does NOT contain `lark-mcp` or hardcoded credentials
    - .agent/README.md exists and contains string `Antigravity`
    - .agent/README.md contains string `## Setup`
    - .agent/README.md contains string `.planning/codebase/`
  </acceptance_criteria>
  <done>.agent/ directory fully populated: 3 context files mirroring .planning/codebase/, GSD skill stub, MCP config with 3 servers, and README with setup instructions.</done>
</task>

</tasks>

<verification>
- `ls .agent/context/ .agent/skills/ .agent/mcp/` shows all subdirectories with files
- `node -e "JSON.parse(require('fs').readFileSync('.agent/mcp/mcp.json','utf8'))"` exits 0
- `diff <(grep context7 .mcp.json) <(grep context7 .agent/mcp/mcp.json)` shows no diff (same servers)
- `pnpm build` still passes
</verification>

<success_criteria>
- .agent/ has context/, skills/, mcp/ subdirectories — all populated
- Context files are verbatim copies of .planning/codebase/ originals
- MCP config has the same 3 servers as project .mcp.json
- README explains Antigravity setup with concrete steps
</success_criteria>

<output>
After completion, create `.planning/phases/04-agent-configs-documentation/04-02-SUMMARY.md`
</output>
