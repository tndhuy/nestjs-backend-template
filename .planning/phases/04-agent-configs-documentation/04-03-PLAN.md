---
phase: 04-agent-configs-documentation
plan: 03
type: execute
wave: 2
depends_on: [04-01]
files_modified:
  - README.md
  - docs/ARCHITECTURE.md
  - docs/CONTRIBUTING.md
  - docs/API.md
autonomous: true
requirements: [REQ-009]

must_haves:
  truths:
    - "New developer can run the service from README alone — clone, install, configure .env, start"
    - "docs/ARCHITECTURE.md explains all 4 DDD layers with concrete examples from ExampleModule"
    - "docs/CONTRIBUTING.md has step-by-step 'add a new module' walkthrough with code snippets"
    - "docs/API.md documents response envelope, error codes, pagination, Swagger decorator patterns"
  artifacts:
    - path: "README.md"
      provides: "Project overview and quick start"
      contains: "Quick Start"
    - path: "docs/ARCHITECTURE.md"
      provides: "DDD architecture guide"
      contains: "Domain Layer"
    - path: "docs/CONTRIBUTING.md"
      provides: "Module creation walkthrough"
      contains: "Add a New Module"
    - path: "docs/API.md"
      provides: "API conventions guide"
      contains: "Response Envelope"
  key_links:
    - from: "README.md"
      to: "docs/ARCHITECTURE.md"
      via: "Architecture section pointer"
      pattern: "docs/ARCHITECTURE.md"
    - from: "README.md"
      to: "docs/CONTRIBUTING.md"
      via: "Adding a New Module section pointer"
      pattern: "docs/CONTRIBUTING.md"
    - from: "docs/CONTRIBUTING.md"
      to: "src/example/"
      via: "Code snippet references throughout walkthrough"
      pattern: "src/example"
    - from: "docs/API.md"
      to: "src/shared/"
      via: "DTO and exception class references"
      pattern: "AppException"
---

<objective>
Write complete developer documentation: README.md (project overview, quick start), docs/ARCHITECTURE.md (DDD layers guide), docs/CONTRIBUTING.md (add-a-module walkthrough with code snippets), docs/API.md (API conventions guide).

Purpose: A new developer can onboard to this template, understand DDD conventions, add a new domain module, and follow API standards — all from documentation alone.

Output: README.md (rewrite), docs/ARCHITECTURE.md, docs/CONTRIBUTING.md, docs/API.md
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-agent-configs-documentation/04-CONTEXT.md
@.planning/phases/04-agent-configs-documentation/04-RESEARCH.md
@.planning/codebase/ARCHITECTURE.md
@.planning/codebase/CONVENTIONS.md
@.planning/codebase/STRUCTURE.md
@.planning/codebase/STACK.md
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write README.md and docs/ARCHITECTURE.md</name>
  <files>README.md, docs/ARCHITECTURE.md</files>
  <read_first>
    - README.md (current content — will be overwritten with complete template README)
    - .planning/codebase/ARCHITECTURE.md (DDD layer architecture — source for docs/ARCHITECTURE.md)
    - .planning/codebase/STACK.md (tech stack with versions — for README tech stack section)
    - .planning/codebase/STRUCTURE.md (file/directory structure — for ARCHITECTURE.md directory tree)
    - .planning/phases/04-agent-configs-documentation/04-RESEARCH.md (README.md structure template in "README.md Structure" section)
    - .env.example (env var names and descriptions — for README env vars table)
    - package.json (pnpm scripts — for README available scripts table)
    - CLAUDE.md (references docs/ — ensure README pointers are consistent)
  </read_first>
  <action>
    **README.md (rewrite entirely):**

    Follow the structure from 04-RESEARCH.md "README.md Structure" section:

    1. `# nestjs-backend-template` — Title.

    2. `## Overview` — 3 bullet points: (a) Production-ready NestJS 11 template with DDD architecture, (b) Pre-configured infrastructure: Prisma, Redis, OpenTelemetry, Scalar API docs, (c) Agent-ready: Claude Code and Antigravity configs pre-wired.

    3. `## Quick Start` — 5 steps:
       Step 1: `git clone <repo-url> my-service && cd my-service`
       Step 2: `pnpm install`
       Step 3: `cp .env.example .env` and edit DATABASE_URL, REDIS_URL
       Step 4: `docker compose up -d` (Postgres + Redis)
       Step 5: `pnpm dev` — server starts at http://localhost:3000, docs at http://localhost:3000/docs

    4. `## Available Scripts` — Table with columns: Script | Description. Include: `pnpm dev`, `pnpm build`, `pnpm start:prod`, `pnpm test`, `pnpm lint`, `pnpm format`. Get exact script names from package.json.

    5. `## Environment Variables` — Table with columns: Variable | Required | Description | Default. Source from .env.example. Include ALL vars from .env.example including the new LARK_APP_ID and LARK_APP_SECRET.

    6. `## Architecture` — Brief paragraph + pointer: "This template follows Domain-Driven Design (DDD) with four layers. See [docs/ARCHITECTURE.md](docs/ARCHITECTURE.md) for the full guide."

    7. `## Adding a New Module` — Brief paragraph + pointer: "Follow the step-by-step walkthrough in [docs/CONTRIBUTING.md](docs/CONTRIBUTING.md)."

    8. `## Tech Stack` — Bullet list with versions from .planning/codebase/STACK.md: NestJS 11, Prisma, ioredis, Pino, OpenTelemetry, Scalar, class-validator, class-transformer.

    9. `## License` — "MIT" (or whatever exists in package.json).

    **docs/ARCHITECTURE.md:**

    Create `docs/` directory if it does not exist.

    Write a developer-oriented DDD architecture guide with these sections:

    1. `# Architecture Guide` — Purpose statement.

    2. `## DDD Layer Overview` — Text diagram showing the 4 layers and their allowed dependencies (domain at bottom, presenter at top). ASCII or markdown diagram.

    3. `## Domain Layer` — What goes here (entities, value objects, domain events, repository interfaces). Concrete examples from ExampleModule: `src/example/domain/item.entity.ts`, `src/example/domain/item-name.value-object.ts`, `src/example/domain/item.repository.interface.ts`. Explain: pure TypeScript, no framework imports.

    4. `## Application Layer` — What goes here (commands, queries, handlers, DTOs). Concrete examples: `src/example/application/commands/create-item.command.ts`, `src/example/application/queries/get-item.handler.ts`, `src/example/application/dtos/`. Explain: orchestrates domain, no infrastructure imports.

    5. `## Infrastructure Layer` — What goes here (Prisma repositories, external service adapters). Concrete examples: `src/example/infrastructure/persistence/prisma-item.repository.ts`. Explain: implements domain interfaces using Prisma/Redis/etc.

    6. `## Presenter Layer` — What goes here (controllers, Swagger decorators). Concrete examples: `src/example/presenter/item.controller.ts`. Explain: HTTP concerns only, delegates to application layer.

    7. `## Shared Code` — `src/shared/` (DDD base classes, DTOs, exceptions) and `src/common/` (filters, interceptors, decorators, middleware). List key files.

    8. `## Data Flow` — Describe a typical request flow: HTTP request -> Controller -> Command/QueryBus -> Handler -> Repository -> Database -> Response envelope.

    9. `## Directory Structure` — Show the full `src/` tree from .planning/codebase/STRUCTURE.md, annotated with layer labels.
  </action>
  <verify>
    <automated>cd /Users/trannguyendanghuy/Workspace/happyland-of-draken/nestjs-backend-template && grep "Quick Start" README.md && grep "docs/ARCHITECTURE.md" README.md && grep "docs/CONTRIBUTING.md" README.md && ls docs/ARCHITECTURE.md && grep "Domain Layer" docs/ARCHITECTURE.md && grep "src/example" docs/ARCHITECTURE.md && grep "Presenter Layer" docs/ARCHITECTURE.md</automated>
  </verify>
  <acceptance_criteria>
    - README.md contains heading `## Quick Start` with 5 numbered steps
    - README.md contains heading `## Available Scripts` with a table
    - README.md contains heading `## Environment Variables` with a table
    - README.md contains link `docs/ARCHITECTURE.md`
    - README.md contains link `docs/CONTRIBUTING.md`
    - README.md contains string `pnpm dev`
    - README.md contains string `DATABASE_URL`
    - docs/ARCHITECTURE.md exists
    - docs/ARCHITECTURE.md contains heading `## Domain Layer`
    - docs/ARCHITECTURE.md contains heading `## Application Layer`
    - docs/ARCHITECTURE.md contains heading `## Infrastructure Layer`
    - docs/ARCHITECTURE.md contains heading `## Presenter Layer`
    - docs/ARCHITECTURE.md contains string `src/example/domain/item.entity.ts`
    - docs/ARCHITECTURE.md contains string `src/example/presenter/item.controller.ts`
  </acceptance_criteria>
  <done>README.md is a complete template README with quick start, scripts, env vars, and doc pointers. docs/ARCHITECTURE.md explains all 4 DDD layers with concrete ExampleModule file references.</done>
</task>

<task type="auto">
  <name>Task 2: Write docs/CONTRIBUTING.md and docs/API.md</name>
  <files>docs/CONTRIBUTING.md, docs/API.md</files>
  <read_first>
    - .planning/phases/04-agent-configs-documentation/04-CONTEXT.md (D-07 API.md scope, D-08 + D-09 CONTRIBUTING.md walkthrough requirements)
    - .planning/phases/04-agent-configs-documentation/04-RESEARCH.md ("docs/CONTRIBUTING.md Walkthrough Sections" and "docs/API.md Conventions Guide Sections")
    - .planning/codebase/CONVENTIONS.md (naming conventions and structural rules)
    - src/example/domain/item.entity.ts (reference entity — copy key code snippets)
    - src/example/domain/item-name.value-object.ts (reference value object — copy key code snippets)
    - src/example/domain/item.repository.interface.ts (reference repository port — copy code snippet)
    - src/example/application/commands/create-item.command.ts (reference command — copy code snippet)
    - src/example/application/commands/create-item.handler.ts (reference handler — copy code snippet)
    - src/example/application/queries/get-item.handler.ts (reference query handler — copy code snippet)
    - src/example/application/dtos/ (list files, read request/response DTOs for code snippets)
    - src/example/infrastructure/persistence/prisma-item.repository.ts (reference Prisma repo — copy code snippet)
    - src/example/presenter/item.controller.ts (reference controller — copy code snippet for Swagger decorators)
    - src/example/example.module.ts (reference module wiring — copy code snippet)
    - src/shared/exceptions/app.exception.ts (AppException class — for API.md error codes section)
    - src/shared/dtos/pagination.dto.ts (PaginationDto — for API.md pagination section)
    - src/common/interceptors/transform.interceptor.ts (response envelope — for API.md envelope section)
    - src/common/decorators/ (list files, read @RawResponse decorator for API.md)
  </read_first>
  <action>
    **docs/CONTRIBUTING.md (per D-08 and D-09):**

    Write a combined contributing guide + onboarding walkthrough with these sections:

    1. `# Contributing Guide` — Purpose: how to extend this template with new domain modules.

    2. `## Prerequisites` — pnpm installed, Docker for Postgres + Redis, understanding of DDD basics.

    3. `## Add a New Module` — The main walkthrough (per D-09). Nine steps, each with:
       - A heading (`### Step N: ...`)
       - Explanation of what and why
       - A code snippet copied from the ExampleModule reference file
       - The file path where the new code should be created

       Steps (per 04-RESEARCH.md "docs/CONTRIBUTING.md Walkthrough Sections"):
       1. **Create the domain entity** — Reference: `src/example/domain/item.entity.ts`. Show how to extend `AggregateRoot`, define props, create static factory method.
       2. **Create value objects** — Reference: `src/example/domain/item-name.value-object.ts`. Show how to extend `StringValueObject` with validation.
       3. **Define the repository interface** — Reference: `src/example/domain/item.repository.interface.ts`. Show the port pattern extending `Repository<T>`.
       4. **Create a CQRS command + handler** — Reference: `src/example/application/commands/create-item.command.ts` and `create-item.handler.ts`. Show command class and `@CommandHandler` decorator.
       5. **Create a CQRS query + handler** — Reference: `src/example/application/queries/get-item.handler.ts`. Show query class and `@QueryHandler` decorator.
       6. **Create application DTOs** — Reference: `src/example/application/dtos/`. Show request/response DTO with class-validator decorators.
       7. **Implement the Prisma repository** — Reference: `src/example/infrastructure/persistence/prisma-item.repository.ts`. Show how the infrastructure adapter implements the domain interface.
       8. **Create the controller** — Reference: `src/example/presenter/item.controller.ts`. Show Swagger decorators: `@ApiTags`, `@ApiOperation`, `@ApiResponse`, `@RawResponse()` when bypassing envelope.
       9. **Wire the module** — Reference: `src/example/example.module.ts`. Show NestModule declaration with providers array, imports, and how to register in `src/app.module.ts`.

    4. `## Code Style` — Pointer to conventions: naming (kebab-case files, PascalCase classes), barrel exports via index.ts, DDD import rules (repeat the layer table from CLAUDE.md).

    5. `## Commit Conventions` — Conventional commits: `feat(module):`, `fix(module):`, `refactor(module):`.

    **docs/API.md (per D-07):**

    Write an API conventions guide (NOT endpoint reference — Scalar at `/docs` covers that):

    1. `# API Conventions Guide` — Purpose: standards for all API endpoints in this template.

    2. `## Response Envelope` — Show the TypeScript interface for the response envelope `{ success: boolean, data: T, meta?: PaginationMeta, error?: ErrorDetail }`. Explain that `TransformInterceptor` wraps all responses automatically. Show how to bypass with `@RawResponse()` decorator (reference: health controller).

    3. `## Error Handling` — Show `AppException` class usage. List built-in error codes from `src/shared/exceptions/`. Show example: `throw new AppException('ITEM_NOT_FOUND', 'Item not found', HttpStatus.NOT_FOUND)`. Show the error response shape `{ success: false, error: { code, message, statusCode, details } }`.

    4. `## Pagination` — Show `PaginationDto` query params (page, limit, sort, order) with class-validator decorators. Show `PaginationMeta` response shape `{ total, page, limit, totalPages }`. Show controller usage pattern.

    5. `## Swagger Decorators` — Patterns for documenting endpoints:
       - `@ApiTags('module-name')` on controller class
       - `@ApiOperation({ summary: '...' })` on each method
       - `@ApiResponse({ status: 200, type: ResponseDto })` for success
       - `@ApiResponse({ status: 404, description: '...' })` for errors
       - Show a complete controller method example from ExampleModule.

    6. `## Versioning` — URI versioning at `/api/v1/`. Controlled by `@Controller({ version: '1' })` or global default. All endpoints automatically prefixed.

    7. `## Rate Limiting` — Default ThrottlerModule config from env vars (THROTTLE_TTL, THROTTLE_LIMIT). How to override per-endpoint with `@Throttle()` decorator.

    8. `## Validation` — `class-validator` decorators required on all request DTOs. Global `ValidationPipe` with `whitelist: true` and `transform: true`. Show example DTO with decorators.
  </action>
  <verify>
    <automated>cd /Users/trannguyendanghuy/Workspace/happyland-of-draken/nestjs-backend-template && ls docs/CONTRIBUTING.md docs/API.md && grep "Add a New Module" docs/CONTRIBUTING.md && grep "src/example/domain/item.entity.ts" docs/CONTRIBUTING.md && grep "Step 9" docs/CONTRIBUTING.md && grep "Response Envelope" docs/API.md && grep "AppException" docs/API.md && grep "PaginationDto" docs/API.md && grep "@ApiTags" docs/API.md && grep "@RawResponse" docs/API.md</automated>
  </verify>
  <acceptance_criteria>
    - docs/CONTRIBUTING.md exists
    - docs/CONTRIBUTING.md contains heading `## Add a New Module`
    - docs/CONTRIBUTING.md contains `### Step 1` through `### Step 9` (all 9 steps present)
    - docs/CONTRIBUTING.md contains string `src/example/domain/item.entity.ts`
    - docs/CONTRIBUTING.md contains string `src/example/presenter/item.controller.ts`
    - docs/CONTRIBUTING.md contains string `src/example/example.module.ts`
    - docs/CONTRIBUTING.md contains code snippets (at least 5 fenced code blocks with ```typescript)
    - docs/CONTRIBUTING.md contains string `app.module.ts` (Step 9 wiring)
    - docs/API.md exists
    - docs/API.md contains heading `## Response Envelope`
    - docs/API.md contains heading `## Error Handling`
    - docs/API.md contains heading `## Pagination`
    - docs/API.md contains heading `## Swagger Decorators`
    - docs/API.md contains string `AppException`
    - docs/API.md contains string `PaginationDto`
    - docs/API.md contains string `@ApiTags`
    - docs/API.md contains string `@RawResponse`
    - docs/API.md does NOT list individual endpoint URLs (that is Scalar's job per D-07)
  </acceptance_criteria>
  <done>docs/CONTRIBUTING.md has 9-step module creation walkthrough with code snippets from ExampleModule. docs/API.md covers response envelope, error codes, pagination, Swagger patterns, versioning, rate limiting, and validation — as a conventions guide, not endpoint reference.</done>
</task>

</tasks>

<verification>
- `ls docs/ARCHITECTURE.md docs/CONTRIBUTING.md docs/API.md` exits 0
- `grep "Quick Start" README.md` finds match
- `grep -c "### Step" docs/CONTRIBUTING.md` returns 9
- `grep "Response Envelope" docs/API.md` finds match
- `pnpm build` still passes (no TS changes)
</verification>

<success_criteria>
- README.md has quick start, scripts, env vars, doc pointers — usable by a new developer alone
- docs/ARCHITECTURE.md explains all 4 DDD layers with ExampleModule file references
- docs/CONTRIBUTING.md has complete 9-step module creation walkthrough with code snippets
- docs/API.md documents conventions (envelope, errors, pagination, Swagger) without listing endpoints
</success_criteria>

<output>
After completion, create `.planning/phases/04-agent-configs-documentation/04-03-SUMMARY.md`
</output>
