---
phase: 02-ddd-foundation-infrastructure
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - nestjs-backend-template/src/shared/base/entity.ts
  - nestjs-backend-template/src/shared/base/aggregate-root.ts
  - nestjs-backend-template/src/shared/base/repository.interface.ts
  - nestjs-backend-template/src/shared/base/index.ts
  - nestjs-backend-template/src/shared/base/entity.spec.ts
  - nestjs-backend-template/src/shared/base/aggregate-root.spec.ts
  - nestjs-backend-template/src/shared/base/value-object.spec.ts
  - nestjs-backend-template/src/shared/valueobjects/id.valueobject.ts
  - nestjs-backend-template/src/shared/valueobjects/string.valueobject.ts
  - nestjs-backend-template/src/shared/valueobjects/number.valueobject.ts
  - nestjs-backend-template/src/shared/valueobjects/date.valueobject.ts
  - nestjs-backend-template/src/shared/valueobjects/index.ts
  - nestjs-backend-template/src/shared/valueobjects/id.valueobject.spec.ts
  - nestjs-backend-template/src/shared/index.ts
autonomous: true
requirements:
  - REQ-002

must_haves:
  truths:
    - "Entity<TId> provides id property and equals() identity comparison"
    - "AggregateRoot<TId> extends Entity and collects/dispatches domain events"
    - "ValueObject<TProps> provides structural equality via equals()"
    - "ValueObject props are immutable after construction (Object.freeze)"
    - "Repository<T, TId> interface defines findById, findAll, save, delete"
    - "Built-in VOs (Id, String, Number, Date) validate and wrap primitives"
    - "All base classes have zero NestJS imports"
  artifacts:
    - path: "nestjs-backend-template/src/shared/base/entity.ts"
      provides: "Entity<TId> base class"
      contains: "export abstract class Entity"
    - path: "nestjs-backend-template/src/shared/base/aggregate-root.ts"
      provides: "AggregateRoot<TId> extending Entity"
      contains: "extends Entity"
    - path: "nestjs-backend-template/src/shared/base/repository.interface.ts"
      provides: "Generic Repository interface"
      contains: "export interface Repository"
    - path: "nestjs-backend-template/src/shared/valueobjects/id.valueobject.ts"
      provides: "IdValueObject"
      contains: "extends ValueObject"
    - path: "nestjs-backend-template/src/shared/base/entity.spec.ts"
      provides: "Entity unit tests"
      contains: "describe('Entity'"
    - path: "nestjs-backend-template/src/shared/base/aggregate-root.spec.ts"
      provides: "AggregateRoot unit tests"
      contains: "describe('AggregateRoot'"
    - path: "nestjs-backend-template/src/shared/base/value-object.spec.ts"
      provides: "ValueObject unit tests including immutability"
      contains: "describe('ValueObject'"
  key_links:
    - from: "nestjs-backend-template/src/shared/base/aggregate-root.ts"
      to: "nestjs-backend-template/src/shared/base/entity.ts"
      via: "extends Entity<TId>"
      pattern: "extends Entity"
    - from: "nestjs-backend-template/src/shared/valueobjects/id.valueobject.ts"
      to: "nestjs-backend-template/src/shared/base/value-object.ts"
      via: "extends ValueObject"
      pattern: "extends ValueObject"
    - from: "nestjs-backend-template/src/shared/index.ts"
      to: "all base classes and VOs"
      via: "barrel exports"
      pattern: "export.*from"
---

<objective>
Implement full DDD base class hierarchy and built-in Value Objects with unit tests.

Purpose: Establish the reusable DDD primitives that all domain modules in the template will build upon. These are framework-free (zero NestJS imports) so domains remain testable in isolation.
Output: Entity, AggregateRoot (refactored), Repository interface, 4 built-in VOs, unit tests, barrel exports.
</objective>

<execution_context>
@.claude/get-shit-done/workflows/execute-plan.md
@.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-ddd-foundation-infrastructure/02-CONTEXT.md
@.planning/phases/02-ddd-foundation-infrastructure/02-RESEARCH.md

<interfaces>
<!-- Current Phase 1 base classes the executor MUST read before modifying -->

From nestjs-backend-template/src/shared/base/aggregate-root.ts:
```typescript
import { DomainEvent } from './domain-event';

export abstract class AggregateRoot<TId> {
  readonly id: TId;
  private readonly domainEvents: DomainEvent[] = [];

  protected constructor(id: TId) {
    this.id = id;
  }

  addDomainEvent(event: DomainEvent): void {
    this.domainEvents.push(event);
  }

  pullDomainEvents(): DomainEvent[] {
    const events = [...this.domainEvents];
    this.domainEvents.length = 0;
    return events;
  }
}
```

From nestjs-backend-template/src/shared/base/value-object.ts:
```typescript
export abstract class ValueObject<TProps> {
  protected readonly props: TProps;

  protected constructor(props: TProps) {
    this.props = Object.freeze({ ...props });
  }

  equals(other?: ValueObject<TProps>): boolean {
    if (!other) return false;
    if (other === this) return true;
    return JSON.stringify(this.props) === JSON.stringify(other.props);
  }
}
```

From nestjs-backend-template/src/shared/base/domain-event.ts:
```typescript
export abstract class DomainEvent {
  readonly occurredAt: Date;
  protected constructor() {
    this.occurredAt = new Date();
  }
}
```

From nestjs-backend-template/src/shared/base/index.ts:
```typescript
export { AggregateRoot } from './aggregate-root';
export { ValueObject } from './value-object';
export { DomainEvent } from './domain-event';
```

From nestjs-backend-template/src/example/domain/item-name.value-object.ts:
```typescript
import { ValueObject } from '../../shared/base/value-object';

interface ItemNameProps {
  value: string;
}

export class ItemName extends ValueObject<ItemNameProps> {
  static create(value: string): ItemName {
    if (!value || value.trim().length === 0) {
      throw new Error('ItemName cannot be empty');
    }
    return new ItemName({ value: value.trim() });
  }

  get value(): string {
    return this.props.value;
  }
}
```

Reference VO pattern from flash-pick-service/src/shared/valueobjects/id.valueobject.ts:
```typescript
export class IdValueObject extends ValueObject<{ value: string }> {
  constructor(value: string) {
    if (!value || value.trim().length === 0) {
      throw new Error('ID must not be empty');
    }
    super({ value });
  }

  get value(): string {
    return this.props.value;
  }
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create Entity base class, refactor AggregateRoot, add Repository interface + unit tests</name>
  <files>
    nestjs-backend-template/src/shared/base/entity.ts,
    nestjs-backend-template/src/shared/base/aggregate-root.ts,
    nestjs-backend-template/src/shared/base/repository.interface.ts,
    nestjs-backend-template/src/shared/base/index.ts,
    nestjs-backend-template/src/shared/base/entity.spec.ts,
    nestjs-backend-template/src/shared/base/aggregate-root.spec.ts,
    nestjs-backend-template/src/shared/base/value-object.spec.ts
  </files>
  <read_first>
    nestjs-backend-template/src/shared/base/aggregate-root.ts,
    nestjs-backend-template/src/shared/base/value-object.ts,
    nestjs-backend-template/src/shared/base/domain-event.ts,
    nestjs-backend-template/src/shared/base/index.ts,
    nestjs-backend-template/src/example/domain/item.entity.ts
  </read_first>
  <behavior>
    - Entity equality: two entities with same id are equal (entity1.equals(entity2) === true)
    - Entity inequality: two entities with different ids are not equal
    - Entity null check: entity.equals(undefined) === false
    - Entity reference equality: entity.equals(entity) === true (same ref)
    - AggregateRoot extends Entity: aggregateRoot.id returns the id passed to constructor
    - AggregateRoot addDomainEvent: after adding event, pullDomainEvents returns array containing it
    - AggregateRoot pullDomainEvents clears: second call to pullDomainEvents returns empty array
    - ValueObject structural equality: two VOs with same props are equal
    - ValueObject structural inequality: two VOs with different props are not equal
    - ValueObject null check: vo.equals(undefined) === false
    - ValueObject immutability: attempting `(vo.props as any).value = 'mutated'` does NOT change the original value (Object.freeze prevents mutation)
  </behavior>
  <action>
    **Per D-01:** Create `nestjs-backend-template/src/shared/base/entity.ts`:
    ```typescript
    export abstract class Entity<TId> {
      readonly id: TId;

      protected constructor(id: TId) {
        this.id = id;
      }

      equals(other?: Entity<TId>): boolean {
        if (!other) return false;
        if (other === this) return true;
        return this.id === other.id;
      }
    }
    ```

    **Per D-02, D-03:** Refactor `nestjs-backend-template/src/shared/base/aggregate-root.ts` to extend Entity:
    ```typescript
    import { Entity } from './entity';
    import { DomainEvent } from './domain-event';

    export abstract class AggregateRoot<TId> extends Entity<TId> {
      private readonly domainEvents: DomainEvent[] = [];

      protected constructor(id: TId) {
        super(id);
      }

      addDomainEvent(event: DomainEvent): void {
        this.domainEvents.push(event);
      }

      pullDomainEvents(): DomainEvent[] {
        const events = [...this.domainEvents];
        this.domainEvents.length = 0;
        return events;
      }
    }
    ```

    CRITICAL: `super(id)` call in AggregateRoot constructor is required. Without it, `this.id` is undefined (Pitfall 1 from RESEARCH.md).

    **Per D-04:** `domain-event.ts` stays as-is. No changes.

    **Per D-05:** Create `nestjs-backend-template/src/shared/base/repository.interface.ts`:
    ```typescript
    export interface Repository<T, TId> {
      findById(id: TId): Promise<T | null>;
      findAll(): Promise<T[]>;
      save(entity: T): Promise<void>;
      delete(id: TId): Promise<void>;
    }
    ```

    **Update** `nestjs-backend-template/src/shared/base/index.ts` to export all:
    ```typescript
    export { Entity } from './entity';
    export { AggregateRoot } from './aggregate-root';
    export { ValueObject } from './value-object';
    export { DomainEvent } from './domain-event';
    export { Repository } from './repository.interface';
    ```

    **Per D-26, D-27:** Create test files:
    - `entity.spec.ts`: Test Entity equality (same id equal, different id not equal, null check, reference equality). Create a concrete `TestEntity extends Entity<string>` for testing.
    - `aggregate-root.spec.ts`: Test AggregateRoot extends Entity (has id), addDomainEvent + pullDomainEvents clears after pull. Create concrete `TestAggregate extends AggregateRoot<string>` and `TestEvent extends DomainEvent`.
    - `value-object.spec.ts`: Test ValueObject structural equality (same props equal, different props not equal, null check). Create concrete `TestVO extends ValueObject<{ value: string }>`.

    **Addresses review concern: ValueObject immutability not tested.** Add an explicit immutability test in `value-object.spec.ts`:
    ```typescript
    it('should be immutable — mutation attempt does not change value', () => {
      const vo = new TestVO({ value: 'original' });
      // Attempt to mutate the frozen props
      expect(() => {
        (vo as any).props.value = 'mutated';
      }).toThrow(); // Object.freeze causes TypeError in strict mode
      // OR if strict mode is not enforced, verify value is unchanged:
      expect(vo.equals(new TestVO({ value: 'original' }))).toBe(true);
    });
    ```
    Note: Jest runs in strict mode by default, so assigning to a frozen property throws TypeError. Test should use `expect(() => { ... }).toThrow()` pattern. As a safety net, also assert the original value is retained.

    TDD: Write all test files FIRST (RED), then implement entity.ts + refactor aggregate-root.ts + repository.interface.ts (GREEN).
  </action>
  <verify>
    <automated>cd nestjs-backend-template && pnpm test --testPathPattern="(entity|aggregate-root|value-object)\\.spec" --no-coverage 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `nestjs-backend-template/src/shared/base/entity.ts` contains `export abstract class Entity<TId>`
    - `nestjs-backend-template/src/shared/base/entity.ts` contains `equals(other?: Entity<TId>): boolean`
    - `nestjs-backend-template/src/shared/base/aggregate-root.ts` contains `extends Entity<TId>`
    - `nestjs-backend-template/src/shared/base/aggregate-root.ts` contains `super(id)`
    - `nestjs-backend-template/src/shared/base/repository.interface.ts` contains `export interface Repository<T, TId>`
    - `nestjs-backend-template/src/shared/base/repository.interface.ts` contains `findById(id: TId): Promise<T | null>`
    - `nestjs-backend-template/src/shared/base/index.ts` contains `export { Entity } from './entity'`
    - `nestjs-backend-template/src/shared/base/index.ts` contains `export { Repository } from './repository.interface'`
    - `nestjs-backend-template/src/shared/base/entity.spec.ts` contains `describe('Entity'`
    - `nestjs-backend-template/src/shared/base/aggregate-root.spec.ts` contains `describe('AggregateRoot'`
    - `nestjs-backend-template/src/shared/base/value-object.spec.ts` contains `describe('ValueObject'`
    - `nestjs-backend-template/src/shared/base/value-object.spec.ts` contains `immutable` (immutability test present)
    - Zero NestJS imports in entity.ts, aggregate-root.ts, value-object.ts, domain-event.ts, repository.interface.ts (grep for `@nestjs` returns 0 matches)
    - `pnpm test --testPathPattern="(entity|aggregate-root|value-object)\\.spec"` exits 0
  </acceptance_criteria>
  <done>Entity base class with equals(), AggregateRoot refactored to extend Entity, Repository interface defined, all 3 test files pass including ValueObject immutability test</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create built-in Value Objects (Id, String, Number, Date) with unit tests + barrel exports</name>
  <files>
    nestjs-backend-template/src/shared/valueobjects/id.valueobject.ts,
    nestjs-backend-template/src/shared/valueobjects/string.valueobject.ts,
    nestjs-backend-template/src/shared/valueobjects/number.valueobject.ts,
    nestjs-backend-template/src/shared/valueobjects/date.valueobject.ts,
    nestjs-backend-template/src/shared/valueobjects/index.ts,
    nestjs-backend-template/src/shared/valueobjects/id.valueobject.spec.ts,
    nestjs-backend-template/src/shared/index.ts
  </files>
  <read_first>
    nestjs-backend-template/src/shared/base/value-object.ts,
    nestjs-backend-template/src/example/domain/item-name.value-object.ts,
    flash-pick-service/src/shared/valueobjects/id.valueobject.ts,
    flash-pick-service/src/shared/valueobjects/string.valueobject.ts
  </read_first>
  <behavior>
    - IdValueObject: constructor('abc') stores value, .value returns 'abc'
    - IdValueObject: constructor('') throws Error('ID must not be empty')
    - IdValueObject: constructor('  ') throws Error('ID must not be empty')
    - StringValueObject: constructor('hello') stores value, .value returns 'hello'
    - StringValueObject: constructor with null/undefined throws
    - NumberValueObject: constructor(42) stores value, .value returns 42
    - NumberValueObject: constructor(NaN) throws Error
    - NumberValueObject: constructor(Infinity) throws Error
    - DateValueObject: constructor(new Date('2024-01-01')) stores value
    - DateValueObject: constructor(new Date('invalid')) throws Error
  </behavior>
  <action>
    **Per D-06, D-07:** Create `nestjs-backend-template/src/shared/valueobjects/` directory with four VOs. Use flash-pick-service VO patterns as reference (constructor validation, `.value` getter).

    **Re: review concern on generic `Error` in ValueObjects:** Keeping generic `Error` for this template. Custom exception classes (e.g., `InvalidIdError`) would add complexity without proportional benefit in a template project. Consumers can override VOs with custom exceptions in their own domain. This is a reasonable trade-off for template simplicity.

    `id.valueobject.ts`:
    ```typescript
    import { ValueObject } from '../base/value-object';

    export class IdValueObject extends ValueObject<{ value: string }> {
      constructor(value: string) {
        if (!value || value.trim().length === 0) {
          throw new Error('ID must not be empty');
        }
        super({ value });
      }

      get value(): string {
        return this.props.value;
      }
    }
    ```

    `string.valueobject.ts`:
    ```typescript
    import { ValueObject } from '../base/value-object';

    export class StringValueObject extends ValueObject<{ value: string }> {
      constructor(value: string) {
        if (value === null || value === undefined) {
          throw new Error('String value must not be null or undefined');
        }
        super({ value });
      }

      get value(): string {
        return this.props.value;
      }
    }
    ```

    `number.valueobject.ts`:
    ```typescript
    import { ValueObject } from '../base/value-object';

    export class NumberValueObject extends ValueObject<{ value: number }> {
      constructor(value: number) {
        if (!Number.isFinite(value)) {
          throw new Error('Number value must be a finite number');
        }
        super({ value });
      }

      get value(): number {
        return this.props.value;
      }
    }
    ```

    `date.valueobject.ts`:
    ```typescript
    import { ValueObject } from '../base/value-object';

    export class DateValueObject extends ValueObject<{ value: Date }> {
      constructor(value: Date) {
        if (!(value instanceof Date) || isNaN(value.getTime())) {
          throw new Error('Date value must be a valid Date');
        }
        super({ value });
      }

      get value(): Date {
        return this.props.value;
      }
    }
    ```

    `index.ts` (barrel):
    ```typescript
    export { IdValueObject } from './id.valueobject';
    export { StringValueObject } from './string.valueobject';
    export { NumberValueObject } from './number.valueobject';
    export { DateValueObject } from './date.valueobject';
    ```

    Create top-level barrel `nestjs-backend-template/src/shared/index.ts`:
    ```typescript
    export * from './base';
    export * from './valueobjects';
    ```

    Create `id.valueobject.spec.ts` with tests for IdValueObject: valid id, empty string throws, whitespace-only throws, two IdValueObjects with same value are equal.

    TDD: Write spec FIRST (RED), then implement VOs (GREEN).
  </action>
  <verify>
    <automated>cd nestjs-backend-template && pnpm test --testPathPattern="id\\.valueobject\\.spec" --no-coverage 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - `nestjs-backend-template/src/shared/valueobjects/id.valueobject.ts` contains `extends ValueObject<{ value: string }>`
    - `nestjs-backend-template/src/shared/valueobjects/id.valueobject.ts` contains `throw new Error('ID must not be empty')`
    - `nestjs-backend-template/src/shared/valueobjects/string.valueobject.ts` contains `extends ValueObject<{ value: string }>`
    - `nestjs-backend-template/src/shared/valueobjects/number.valueobject.ts` contains `Number.isFinite(value)`
    - `nestjs-backend-template/src/shared/valueobjects/date.valueobject.ts` contains `isNaN(value.getTime())`
    - `nestjs-backend-template/src/shared/valueobjects/index.ts` contains `export { IdValueObject }`
    - `nestjs-backend-template/src/shared/index.ts` contains `export * from './base'`
    - `nestjs-backend-template/src/shared/index.ts` contains `export * from './valueobjects'`
    - `pnpm test --testPathPattern="id\\.valueobject\\.spec"` exits 0
    - `pnpm build` exits 0 (no TypeScript errors from new files)
  </acceptance_criteria>
  <done>Four built-in VOs implemented with validation, barrel exports from src/shared/index.ts, IdValueObject spec passes, build succeeds</done>
</task>

</tasks>

<verification>
cd nestjs-backend-template && pnpm test --testPathPattern="shared" --no-coverage
cd nestjs-backend-template && pnpm build
# Verify zero NestJS imports in shared/base:
grep -r "@nestjs" nestjs-backend-template/src/shared/base/ (expect 0 matches)
# Verify immutability test exists:
grep -r "immutable\|mutated" nestjs-backend-template/src/shared/base/value-object.spec.ts (expect match)
</verification>

<success_criteria>
- All 6 base classes/interfaces exportable from `src/shared/index.ts`
- Unit tests pass for Entity equality, ValueObject equality (including immutability), AggregateRoot domain events, IdValueObject validation
- `pnpm build` succeeds with zero errors
- No `@nestjs` imports in `src/shared/base/` or `src/shared/valueobjects/`
</success_criteria>

<output>
After completion, create `.planning/phases/02-ddd-foundation-infrastructure/02-01-SUMMARY.md`
</output>
