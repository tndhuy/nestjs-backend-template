---
phase: 04-agent-configs-documentation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - CLAUDE.md
  - .mcp.json
  - .claude/skills/gsd/SKILL.md
  - .claude/skills/understand-anything/SKILL.md
  - .env.example
autonomous: true
requirements: [REQ-007]

must_haves:
  truths:
    - "CLAUDE.md at project root describes DDD architecture, forbidden patterns, and key decisions for Claude Code agents"
    - "Project .mcp.json at repo root configures context7, prisma, and playwright MCP servers"
    - "Skills directory has GSD and Understand-Anything SKILL.md stubs"
    - ".env.example includes LARK_APP_ID and LARK_APP_SECRET entries"
  artifacts:
    - path: "CLAUDE.md"
      provides: "Agent-optimized project brief"
      contains: "Forbidden Patterns"
    - path: ".mcp.json"
      provides: "Project-level MCP server config"
      contains: "context7"
    - path: ".claude/skills/gsd/SKILL.md"
      provides: "GSD skill stub"
      contains: "name: gsd"
    - path: ".claude/skills/understand-anything/SKILL.md"
      provides: "Understand-Anything skill stub"
      contains: "name: understand-anything"
    - path: ".env.example"
      provides: "Environment variable template"
      contains: "LARK_APP_ID"
  key_links:
    - from: "CLAUDE.md"
      to: "docs/CONTRIBUTING.md"
      via: "pointer in Working with This Codebase section"
      pattern: "docs/CONTRIBUTING.md"
    - from: ".mcp.json"
      to: ".env.example"
      via: "DATABASE_URL env var reference"
      pattern: "DATABASE_URL"
---

<objective>
Configure Claude Code agent tooling: write CLAUDE.md (agent-optimized project brief), create project-level `.mcp.json` with 3 MCP servers, scaffold `.claude/skills/` with GSD and Understand-Anything stubs, and update `.env.example` with Lark credentials.

Purpose: Every developer who opens this project in Claude Code gets pre-wired MCP servers, skill access, and an agent brief that enforces DDD conventions — zero manual setup.

Output: CLAUDE.md, .mcp.json, .claude/skills/gsd/SKILL.md, .claude/skills/understand-anything/SKILL.md, updated .env.example
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-agent-configs-documentation/04-CONTEXT.md
@.planning/phases/04-agent-configs-documentation/04-RESEARCH.md
@.planning/codebase/ARCHITECTURE.md
@.planning/codebase/CONVENTIONS.md
@.planning/codebase/STACK.md
@.planning/codebase/STRUCTURE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create CLAUDE.md agent brief and project .mcp.json</name>
  <files>CLAUDE.md, .mcp.json</files>
  <read_first>
    - .planning/STATE.md (architectural decisions table — must be embedded in CLAUDE.md per D-03)
    - .planning/codebase/ARCHITECTURE.md (DDD layer rules source)
    - .planning/codebase/CONVENTIONS.md (naming conventions source)
    - .planning/codebase/STRUCTURE.md (file structure patterns source)
    - .planning/codebase/STACK.md (tech stack source)
    - .planning/phases/04-agent-configs-documentation/04-CONTEXT.md (locked decisions D-01 through D-06)
    - .planning/phases/04-agent-configs-documentation/04-RESEARCH.md (CLAUDE.md structure template, MCP config JSON, forbidden patterns list)
    - CLAUDE.md (current content — will be overwritten)
    - .claude/.mcp.json (existing user-level config — reference only, do NOT copy credentials)
    - src/example/ (list directory tree to understand module structure for the walkthrough section)
  </read_first>
  <action>
    **CLAUDE.md (project root, ~200-300 lines, per D-01):**

    Write an agent-optimized brief with these exact sections (per D-02, D-03):

    1. `# NestJS Backend Template -- Claude Code Brief` — 1 paragraph: DDD template, NestJS 11, Prisma, Redis, pnpm.

    2. `## Architecture` — DDD layer diagram showing domain/application/infrastructure/presenter. Include the import rules table from 04-RESEARCH.md "DDD Layer Import Rules" section verbatim:
       | Layer | Location | May Import | NEVER Import |
       | Domain | src/*/domain/ | Nothing (pure TS/classes) | @nestjs/*, prisma, ioredis |
       | Application | src/*/application/ | Domain layer only | @nestjs/*, prisma, ioredis |
       | Infrastructure | src/*/infrastructure/ | NestJS DI, Prisma, ioredis | Domain business logic directly |
       | Presenter | src/*/presenter/ | Application DTOs, @nestjs/common | Domain entities directly |

    3. `## Forbidden Patterns` — Bullet list of what agents must NEVER do (per D-02):
       - Never import `@nestjs/*` in any `domain/` file
       - Never import `PrismaService` in any `application/` file
       - Never use `new EntityClass()` in controllers — always go through command/query handler
       - Never hardcode env vars — always use `ConfigService`
       - Never bypass `TransformInterceptor` except with `@RawResponse()` decorator

    4. `## Module Structure` — File-level walkthrough of `src/example/` showing each DDD layer with actual filenames from the codebase. List every file in src/example/ grouped by layer (domain/, application/, infrastructure/, presenter/).

    5. `## Key Architectural Decisions` — Table from STATE.md decisions (per D-03):
       - commonjs over nodenext — reflect-metadata requires it
       - URI versioning at /api/v1/ — team preference
       - NestExpressApplication type — needed for .set('trust proxy', 1)
       - OTel via OTEL_ENABLED=true — dynamic require avoids loading heavy deps
       - start:prod points to dist/src/main — NestJS CLI sourceRoot behavior
       - DDD base classes are framework-free — keeps domain unit-testable

    6. `## Commands Quick Reference` — Table of pnpm scripts: dev, build, test, lint, format, start:prod, docker:up, docker:down.

    7. `## Global MCP Setup (Developer Action Required)` — Per D-05 and D-06, document Lark MCP and Docker MCP as user-global configs. Include the exact JSON snippets from 04-RESEARCH.md "User-Global MCP Config Snippet" section. Instruct developers to add to `~/.claude/.mcp.json`. Use `${LARK_APP_ID}` and `${LARK_APP_SECRET}` env var references — NEVER hardcode credentials.

    8. `## Working with This Codebase` — Pointer to `docs/CONTRIBUTING.md` for the "add a new module" walkthrough.

    **Project .mcp.json (repo root, per D-04):**

    Create `.mcp.json` at project root with exactly this content:
    ```json
    {
      "mcpServers": {
        "context7": {
          "type": "stdio",
          "command": "npx",
          "args": ["-y", "@upstash/context7-mcp"]
        },
        "prisma": {
          "type": "stdio",
          "command": "npx",
          "args": ["-y", "@prisma/mcp-server-prisma"],
          "env": {
            "DATABASE_URL": "${DATABASE_URL}"
          }
        },
        "playwright": {
          "type": "stdio",
          "command": "npx",
          "args": ["-y", "@playwright/mcp"]
        }
      }
    }
    ```

    Do NOT include lark-mcp or Docker MCP in project .mcp.json (per D-05).
    Do NOT copy any hardcoded credentials from .claude/.mcp.json.
  </action>
  <verify>
    <automated>cd /Users/trannguyendanghuy/Workspace/happyland-of-draken/nestjs-backend-template && node -e "JSON.parse(require('fs').readFileSync('.mcp.json','utf8'))" && grep -c "context7" .mcp.json && grep -c "prisma" .mcp.json && grep -c "playwright" .mcp.json && grep "Forbidden Patterns" CLAUDE.md && grep "Key Architectural Decisions" CLAUDE.md && grep "commonjs" CLAUDE.md && wc -l CLAUDE.md</automated>
  </verify>
  <acceptance_criteria>
    - CLAUDE.md exists at project root, between 200-300 lines
    - CLAUDE.md contains heading `## Forbidden Patterns`
    - CLAUDE.md contains heading `## Key Architectural Decisions`
    - CLAUDE.md contains heading `## Module Structure`
    - CLAUDE.md contains heading `## Global MCP Setup`
    - CLAUDE.md contains string `commonjs over nodenext`
    - CLAUDE.md contains string `NestExpressApplication`
    - CLAUDE.md contains string `docs/CONTRIBUTING.md`
    - CLAUDE.md contains `${LARK_APP_ID}` (env var ref, not hardcoded)
    - CLAUDE.md does NOT contain `cli_a921496251b89eed` (no hardcoded credentials)
    - .mcp.json exists at project root (NOT inside .claude/)
    - .mcp.json is valid JSON
    - .mcp.json contains exactly 3 servers: context7, prisma, playwright
    - .mcp.json does NOT contain `lark-mcp` or `MCP_DOCKER`
    - .mcp.json contains `${DATABASE_URL}` env var reference
  </acceptance_criteria>
  <done>CLAUDE.md is 200-300 lines with all 8 sections. .mcp.json has 3 project-level MCP servers. No hardcoded credentials in either file.</done>
</task>

<task type="auto">
  <name>Task 2: Create skill stubs and update .env.example</name>
  <files>.claude/skills/gsd/SKILL.md, .claude/skills/understand-anything/SKILL.md, .env.example</files>
  <read_first>
    - .planning/phases/04-agent-configs-documentation/04-RESEARCH.md (SKILL.md format examples — "GSD SKILL.md Format" and "Understand-Anything SKILL.md Format" sections)
    - .env.example (current content — will be appended to)
  </read_first>
  <action>
    **Create `.claude/skills/gsd/SKILL.md`** with this exact content:
    ```markdown
    ---
    name: gsd
    description: Use for milestone-based development workflows. Invoke /gsd:<command> for planning, research, implementation, and verification of project phases.
    ---

    # GSD Skill

    GSD (Get Shit Done) is a milestone-driven development framework installed globally.

    ## Commands
    - `/gsd:map-codebase` -- map project structure into `.planning/codebase/`
    - `/gsd:discuss-phase` -- gather requirements + decisions for a phase
    - `/gsd:research-phase` -- research implementation approach
    - `/gsd:plan-phase` -- generate task plans
    - `/gsd:do-work` -- execute plans
    - `/gsd:verify-work` -- verify completion

    State lives in `.planning/`. Never delete `.planning/` files.
    ```

    **Create `.claude/skills/understand-anything/SKILL.md`** with this exact content:
    ```markdown
    ---
    name: understand-anything
    description: Use to onboard onto a codebase, understand architecture, or explore unfamiliar code. Run /understand first to build the knowledge graph, then use sub-commands.
    ---

    # Understand-Anything Skill

    Understand-Anything builds a knowledge graph of the codebase for navigation and explanation.

    ## Commands
    - `/understand` -- build knowledge graph (run first)
    - `/understand-onboard` -- generate onboarding guide
    - `/understand-explain` -- explain a specific file or component
    - `/understand-diff` -- understand a diff or PR
    - `/understand-dashboard` -- high-level project overview

    Knowledge graph stored at `.understand-anything/knowledge-graph.json`.
    ```

    **Update `.env.example`** — Append these lines at the end of the existing file (per D-06):
    ```
    # Lark MCP (global agent config — see CLAUDE.md for setup)
    LARK_APP_ID=
    LARK_APP_SECRET=
    ```

    Do NOT remove any existing env vars. Append only.
  </action>
  <verify>
    <automated>cd /Users/trannguyendanghuy/Workspace/happyland-of-draken/nestjs-backend-template && ls .claude/skills/gsd/SKILL.md .claude/skills/understand-anything/SKILL.md && grep "name: gsd" .claude/skills/gsd/SKILL.md && grep "name: understand-anything" .claude/skills/understand-anything/SKILL.md && grep "LARK_APP_ID" .env.example && grep "LARK_APP_SECRET" .env.example && grep "DATABASE_URL" .env.example</automated>
  </verify>
  <acceptance_criteria>
    - .claude/skills/gsd/SKILL.md exists and contains `name: gsd` in frontmatter
    - .claude/skills/gsd/SKILL.md contains `/gsd:map-codebase`
    - .claude/skills/understand-anything/SKILL.md exists and contains `name: understand-anything`
    - .claude/skills/understand-anything/SKILL.md contains `/understand`
    - .env.example contains `LARK_APP_ID=` line
    - .env.example contains `LARK_APP_SECRET=` line
    - .env.example still contains all original vars (DATABASE_URL, REDIS_URL, PORT, etc.)
  </acceptance_criteria>
  <done>Two SKILL.md stubs created. .env.example has LARK_APP_ID and LARK_APP_SECRET appended without losing existing vars.</done>
</task>

</tasks>

<verification>
- `node -e "JSON.parse(require('fs').readFileSync('.mcp.json','utf8'))"` exits 0
- `ls .claude/skills/gsd/SKILL.md .claude/skills/understand-anything/SKILL.md` exits 0
- `grep "Forbidden Patterns" CLAUDE.md` finds match
- `grep "LARK_APP_ID" .env.example` finds match
- `pnpm build` still passes (no TS changes)
</verification>

<success_criteria>
- CLAUDE.md is 200-300 lines, agent-optimized, with all 8 required sections
- .mcp.json at repo root has 3 servers (context7, prisma, playwright), no credentials
- .claude/skills/ has gsd/ and understand-anything/ with valid SKILL.md files
- .env.example has LARK_APP_ID and LARK_APP_SECRET entries
- Zero hardcoded credentials in any file
</success_criteria>

<output>
After completion, create `.planning/phases/04-agent-configs-documentation/04-01-SUMMARY.md`
</output>
